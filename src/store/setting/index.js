import model from 'src/models/settingModel'
import actions from './actions'
import mutations from './mutations'

const state = {
  userSetting: model
}

export default {
  state,
  actions,
  mutations
}
